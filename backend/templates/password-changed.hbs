<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Changed Successfully</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Inter', sans-serif;
            background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
            padding: 40px 20px;
        }
        .email-container {
            max-width: 600px;
            margin: 0 auto;
            background: #ffffff;
            border-radius: 24px;
            overflow: hidden;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        }
        .header {
            background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
            padding: 0;
            position: relative;
            overflow: hidden;
            height: 280px;
        }
        .orb {
            position: absolute;
            border-radius: 50%;
            filter: blur(40px);
            opacity: 0.5;
            animation: float 6s ease-in-out infinite;
        }
        .orb-1 {
            width: 180px;
            height: 180px;
            background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
            top: -40px;
            right: -40px;
            animation-delay: 0s;
        }
        .orb-2 {
            width: 140px;
            height: 140px;
            background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%);
            bottom: -20px;
            left: -20px;
            animation-delay: 2s;
        }
        @keyframes float {
            0%, 100% { transform: translateY(0) scale(1); }
            50% { transform: translateY(-15px) scale(1.08); }
        }
        .checkmark-animation {
            position: absolute;
            width: 8px;
            height: 8px;
            background: rgba(255, 255, 255, 0.7);
            border-radius: 50%;
            animation: checkFloat 3s ease-in-out infinite;
        }
        .check-1 { top: 20%; left: 15%; animation-delay: 0s; }
        .check-2 { top: 70%; left: 80%; animation-delay: 1s; }
        .check-3 { top: 50%; right: 20%; animation-delay: 2s; }
        @keyframes checkFloat {
            0%, 100% { opacity: 0; transform: translateY(0) scale(0.5); }
            50% { opacity: 1; transform: translateY(-25px) scale(1); }
        }
        .header-content {
            position: relative;
            z-index: 10;
            text-align: center;
            padding: 60px 40px;
        }
        .icon-wrapper {
            width: 120px;
            height: 120px;
            margin: 0 auto 20px;
            background: rgba(255, 255, 255, 0.25);
            backdrop-filter: blur(10px);
            border-radius: 30px;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 2px solid rgba(255, 255, 255, 0.4);
            animation: successPulse 2s ease-in-out infinite;
        }
        @keyframes successPulse {
            0%, 100% { transform: scale(1); box-shadow: 0 0 0 0 rgba(255, 255, 255, 0.5); }
            50% { transform: scale(1.05); box-shadow: 0 0 0 15px rgba(255, 255, 255, 0); }
        }
        .icon {
            font-size: 64px;
            animation: iconPop 0.6s ease-out;
        }
        @keyframes iconPop {
            0% { transform: scale(0); }
            50% { transform: scale(1.2); }
            100% { transform: scale(1); }
        }
        .header-title {
            font-size: 38px;
            font-weight: 800;
            color: #ffffff;
            margin: 0 0 10px 0;
            letter-spacing: -1px;
        }
        .header-subtitle {
            font-size: 16px;
            color: rgba(255, 255, 255, 0.95);
            margin: 0;
            font-weight: 500;
        }
        .content {
            padding: 50px 40px;
        }
        .greeting {
            font-size: 24px;
            font-weight: 700;
            color: #1a1a1a;
            margin: 0 0 20px 0;
        }
        .message {
            font-size: 16px;
            color: #666666;
            line-height: 1.8;
            margin: 0 0 25px 0;
        }
        .success-box {
            background: linear-gradient(135deg, #f0fff4 0%, #e6fffa 100%);
            border: 2px solid #43e97b;
            border-radius: 20px;
            padding: 30px;
            text-align: center;
            margin: 30px 0;
            position: relative;
            overflow: hidden;
        }
        .success-box::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(67, 233, 123, 0.1) 0%, transparent 70%);
            animation: rotate 15s linear infinite;
        }
        @keyframes rotate {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        .success-icon {
            font-size: 48px;
            margin: 0 0 15px 0;
            position: relative;
            z-index: 1;
        }
        .success-title {
            font-size: 22px;
            font-weight: 700;
            color: #1a1a1a;
            margin: 0 0 10px 0;
            position: relative;
            z-index: 1;
        }
        .success-text {
            font-size: 15px;
            color: #666666;
            margin: 0;
            position: relative;
            z-index: 1;
        }
        .alert-box {
            background: #fff3cd;
            border-left: 4px solid #ffc107;
            border-radius: 12px;
            padding: 25px;
            margin: 30px 0;
        }
        .alert-header {
            display: flex;
            align-items: center;
            margin-bottom: 12px;
        }
        .alert-icon {
            font-size: 28px;
            margin-right: 12px;
        }
        .alert-title {
            font-size: 16px;
            font-weight: 700;
            color: #856404;
            margin: 0;
        }
        .alert-text {
            font-size: 14px;
            color: #856404;
            margin: 0;
            line-height: 1.6;
        }
        .info-grid {
            display: table;
            width: 100%;
            margin: 30px 0;
        }
        .info-row {
            display: table-row;
        }
        .info-icon {
            display: table-cell;
            width: 50px;
            padding: 12px 0;
            vertical-align: top;
        }
        .info-content {
            display: table-cell;
            padding: 12px 0 12px 10px;
            vertical-align: top;
        }
        .info-icon-circle {
            width: 40px;
            height: 40px;
            border-radius: 10px;
            background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
        }
        .info-title {
            font-size: 14px;
            font-weight: 700;
            color: #1a1a1a;
            margin: 0 0 4px 0;
        }
        .info-desc {
            font-size: 13px;
            color: #666666;
            margin: 0;
            line-height: 1.5;
        }
        .cta-section {
            background: #f8f9fc;
            border-radius: 16px;
            padding: 25px;
            text-align: center;
            margin: 30px 0;
        }
        .cta-text {
            font-size: 15px;
            color: #666666;
            margin: 0 0 20px 0;
        }
        .cta-button {
            display: inline-block;
            padding: 14px 32px;
            background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
            color: #ffffff;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 700;
            font-size: 15px;
            box-shadow: 0 8px 20px rgba(67, 233, 123, 0.3);
        }
        .divider {
            height: 1px;
            background: linear-gradient(90deg, transparent, #e0e0e0, transparent);
            margin: 40px 0;
        }
        .footer {
            background: #f8f9fc;
            padding: 30px 40px;
            text-align: center;
        }
        .footer-text {
            font-size: 13px;
            color: #999999;
            margin: 5px 0;
            line-height: 1.6;
        }
    </style>
</head>
<body>
    <div class="email-container">
        <div class="header">
            <div class="orb orb-1"></div>
            <div class="orb orb-2"></div>
            <div class="checkmark-animation check-1"></div>
            <div class="checkmark-animation check-2"></div>
            <div class="checkmark-animation check-3"></div>
            
            <div class="header-content">
                <div class="icon-wrapper">
                    <span class="icon">✅</span>
                </div>
                <h1 class="header-title">All Set!</h1>
                <p class="header-subtitle">Your password has been updated</p>
            </div>
        </div>
        
        <div class="content">
            <h2 class="greeting">Hello {{name}},</h2>
            
            <div class="success-box">
                <div class="success-icon">🎉</div>
                <h3 class="success-title">Password Changed Successfully</h3>
                <p class="success-text">Your account is now secured with your new password.</p>
            </div>
            
            <p class="message">
                Your password has been successfully updated. You can now use your new password to sign in to your account.
            </p>
            
            <div class="info-grid">
                <div class="info-row">
                    <div class="info-icon">
                        <div class="info-icon-circle">🔐</div>
                    </div>
                    <div class="info-content">
                        <h3 class="info-title">What This Means</h3>
                        <p class="info-desc">Your password has been changed and all existing sessions have been logged out for security.</p>
                    </div>
                </div>
                <div class="info-row">
                    <div class="info-icon">
                        <div class="info-icon-circle">📱</div>
                    </div>
                    <div class="info-content">
                        <h3 class="info-title">Next Steps</h3>
                        <p class="info-desc">Use your new password to sign in on all your devices.</p>
                    </div>
                </div>
            </div>
            
            <div class="alert-box">
                <div class="alert-header">
                    <span class="alert-icon">⚠️</span>
                    <h3 class="alert-title">Didn't Make This Change?</h3>
                </div>
                <p class="alert-text">
                    If you didn't change your password, your account may be compromised. Please contact our support team immediately to secure your account.
                </p>
            </div>
            
            <div class="cta-section">
                <p class="cta-text">Need to contact support?</p>
                <a href="#" class="cta-button">Get Help Now</a>
            </div>
            
            <div class="divider"></div>
            
            <p class="message" style="text-align: center; color: #999999; font-size: 14px;">
                This is a security notification sent to keep you informed about important changes to your account.
            </p>
        </div>
        
        <div class="footer">
            <p class="footer-text">
                <strong>Your security is our priority 🛡️</strong>
            </p>
            <p class="footer-text">
                This is an automated security email. Please do not reply.
            </p>
            <p class="footer-text" style="margin-top: 15px; color: #cccccc;">
                © 2025 All rights reserved.
            </p>
        </div>
    </div>
</body>
</html>